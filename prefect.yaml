name: hello-project
prefect-version: "3.4.1"

build: null
push: null

pull:
  - prefect.deployments.steps.set_working_directory:
      directory: /

deployments:
- name: hello-deployment
  version: "1"
  tags: []
  description: "A simple hello world deployment"
  schedule: {}  # You can later add interval/cron here
  flow_name: hello
  entrypoint: hello_flow.py:hello
  parameters: {}
  work_pool:
    name: "ats_work_pool"  # Make sure this pool exists
    work_queue_name: null
    job_variables: {}